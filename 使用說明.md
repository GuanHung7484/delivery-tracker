# 外送收入管家 — 使用說明

一個給外送員用的每日收入記帳 App。三大平台（Uber Eats、foodpanda、Lalamove）+ 其他，
自動算出時薪、每公里收入、均單量、均單價，並和昨天比較，附圓餅圖。

## 怎麼開啟

- **電腦**：直接用滑鼠點兩下 `index.html`，會用瀏覽器打開。
- **手機**：把 `index.html` 傳到手機（用 LINE 傳給自己、或存到 Google 雲端硬碟），用手機瀏覽器打開。

## 裝成像 App 一樣（加到主畫面）

用手機瀏覽器打開後：

- **iPhone（Safari）**：點下方「分享」按鈕 → 「加入主畫面」。
- **Android（Chrome）**：點右上角「⋮」→ 「加到主畫面 / 安裝應用程式」。

之後桌面就會有一個圖示，點開全螢幕使用，跟真的 App 一樣。

## 怎麼記帳

1. 上方可切換日期（‹ › 箭頭、或「回到今天」）。
2. 按右下角 **✏️ 編輯今日**。
3. 填入：
   - 上線工時（幾小時幾分鐘）
   - 總里程（km）
   - 各平台的「收入」和「單量」（沒跑的平台留空就好）
4. 按「儲存」，所有數字會自動算好。

## 自動算出來的數字

| 項目 | 怎麼算 |
|------|--------|
| 日收入 | 各平台收入加總 |
| 時薪 | 日收入 ÷ 工時 |
| 總單量 | 各平台單量加總 |
| 每公里 | 日收入 ÷ 里程 |
| 均單量 | 總單量 ÷ 工時（每小時幾單）|
| 均單價 | 日收入 ÷ 總單量 |

「vs 昨日」是和前一天的數字比較，紅色▼代表比昨天低，綠色▲代表比昨天高。

## 資料存哪裡？

資料只存在你自己的手機/電腦瀏覽器裡（localStorage），不會上傳到網路。
⚠️ 注意：如果「清除瀏覽器資料 / 快取」可能會把記錄一起清掉。
