# 外送收入管家 — 使用說明（v1.3）

給外送員用的每日收入記帳 App。三大平台（Uber Eats、foodpanda、Lalamove）＋其他，
自動算時薪、每公里、均單量、均單價、**淨收入**，和昨天比較、附圓餅圖，還有**本月統計**與**備份**。

## 怎麼開啟

- **線上版（最方便，手機點開就能用）**：https://guanhung7484.github.io/delivery-tracker/
- **電腦**：滑鼠點兩下 `index.html`，會用瀏覽器打開。
- **手機（檔案版）**：把 `index.html` 傳到手機，用瀏覽器打開。

## 裝成像 App 一樣（加到主畫面）

用手機瀏覽器打開線上版後：
- **iPhone（Safari）**：點下方「分享」→「加入主畫面」。
- **Android（Chrome）**：點右上角「⋮」→「加到主畫面 / 安裝應用程式」。

## 怎麼記帳

1. 上方可切換日期（‹ › 箭頭、或「回到今天」）。
2. 按右下角 **✏️ 編輯今日**。
3. 填入：
   - 上線工時（幾小時幾分鐘）
   - 總里程（**留空會自動估算**，見下方）
   - 每單平均公里（設定一次就好，自動估算用）
   - 當日成本：油錢、其他（保養/停車…，沒有可留空）
   - 各平台的「收入」和「單量」（沒跑的平台留空）
4. 按「儲存」，所有數字自動算好。

## 里程怎麼算（不用每天記）

- **自動估算**：設定一次「每單平均公里」，里程＝單量 × 平均，估算值會標「≈」。
- **想填準**：哪天有看碼表，就把數字填進「總里程」，那天就用實際值。

## 日報 / 月報

上方有「日報 / 月報」兩個頁籤：
- **日報**：當天各平台收入、合計、成本、淨收入、9 格數據、圓餅圖。
- **月報**：本月總收入、出勤天數、平均日收入、平均時薪、總里程、本月成本、本月淨收入、🏆 最佳一天、每日收入長條圖。

## 自動算出來的數字

| 項目 | 怎麼算 |
|------|--------|
| 日收入 | 各平台收入加總 |
| 成本 / 淨收入 | 淨收入 ＝ 日收入 － 成本（油錢＋其他）|
| 時薪 / 淨時薪 | 日收入(或淨收入) ÷ 工時 |
| 總單量 | 各平台單量加總 |
| 里程 / 每公里 | 每公里 ＝ 日收入 ÷ 里程 |
| 均單量 | 總單量 ÷ 工時（每小時幾單）|
| 均單價 | 日收入 ÷ 總單量 |

「vs 昨日」：紅色▼比昨天低、綠色▲比昨天高（要有前一天資料才會顯示）。

## 備份（重要）

主畫面最下面有「💾 資料備份」：
- **⬇️ 匯出備份**：把所有紀錄存成 CSV（可用 Excel 打開）。
- **⬆️ 匯入備份**：選擇之前匯出的 CSV 還原（會覆蓋相同日期）。

➡️ **換手機、或清除瀏覽器前，一定要先匯出！**

## 資料存哪裡？

只存在你自己的手機/電腦瀏覽器（localStorage），不會上傳。
⚠️ 「清除瀏覽器資料 / 快取」可能會把紀錄清掉 → 所以要定期匯出備份。
